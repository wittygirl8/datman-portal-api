const moment = require('moment-timezone');
const payment = require('../../database/model/payment')
const { Sequelize } = require('../../database')
const Op = Sequelize.Op

module.exports.FUNCTION_NAME = (info) => {


    return new Promise((resolve, reject) => {

    })

}
